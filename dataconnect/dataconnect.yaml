specVersion: "v1beta"
serviceId: "newapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
